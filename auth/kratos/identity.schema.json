{
  "$id": "https://flyer.social/schemas/user.schema.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "FlyerUser",
  "type": "object",
  "properties": {
    "traits": {
      "type": "object",
      "properties": {
        "email": {
          "type": "string",
          "format": "email",
          "title": "E-Mail",
          "minLength": 3,
          "ory.sh/kratos": {
            "credentials": {
              "password": {
                "identifier": true
              }
            },
            "verification": {
              "via": "email"
            },
            "recovery": {
              "via": "email"
            }
          }
        },
        "username": {
          "type": "string",
          "title": "Username",
          "minLength": 3,
          "maxLength": 30,
          "pattern": "^[A-Za-z0-9]+(?:[ _-][A-Za-z0-9]+)*$",
          "ory.sh/kratos": {
            "credentials": {
              "password": {
                "identifier": true
              }
            }
          }
        }
      },
      "required": [
        "email", "username"
      ],
      "additionalProperties": false
    }
  }
}
