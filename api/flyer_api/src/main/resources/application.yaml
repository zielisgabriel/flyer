spring:
  profiles:
    active: dev

  application:
    name: flyer

app:
  security:
    jwt:
      secret: ${JWT_SECRET}
      access-token-expiration-ms: 900000
      refresh-token-expiration-ms: 86400000
